import { Container } from 'konva';

export type ContainerPropsType = {
  clipX?: number, clipY?: number,
};

export function applyContainerProps(container: Container, containerProps: ContainerPropsType) {
  // node
  // node.clip({
  //   x: undefined,
  // });
}
